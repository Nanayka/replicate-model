version: "1"
model:
  - name: predict
    command: python3 predict.py
    inputs:
      prompt:
        type: string
        description: "Текстовый запрос для генерации"
    outputs:
      output:
        type: path
        description: "Путь к сгенерированному изображению"
