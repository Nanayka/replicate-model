build:
  gpu: true
  python_version: "3.10"
  python_packages:
    - torch
    - torchvision
    - transformers

predict: predict.py:Predictor

input:
  input_photo:
    type: path
    description: "Ссылка на изображение"
  style:
    type: string
    description: "Стиль для генерации"
