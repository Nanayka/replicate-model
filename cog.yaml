build:
  gpu: true
  python_version: "3.10"
  python_packages:
    - torch
    - torchvision
    - transformers
    - diffusers
    - accelerate
    - safetensors

predict: predict.py:Predictor

inputs:
  input_photo:
    type: path
    description: "Фото пользователя"
  style:
    type: string
    description: "Желаемый стиль"
    default: "in the style of Hollywood glamour"
